import { createjsonConfig } from "json-agent-playwright-config/config";

export default createjsonConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
